# Environment variables for MLOps Text Embedding Pipeline
# Copy this file to .env and fill in your values

# S3 bucket name (overrides config.yaml)
S3_BUCKET_NAME=your-s3-bucket-name

# Pipeline version (optional, defaults to "unknown" if not set)
# This will be automatically set by the build script from git commit hash
PIPELINE_VERSION=unknown
